name: One-time recover consistent legacy notification ownership

on:
  push:
    branches:
      - ops/recover-legacy-owner-20260905

permissions:
  contents: read
  checks: read

jobs:
  recover:
    uses: ./.github/workflows/production-host-core.yml
    with:
      operation: rollback
      target_commit: eacd0b813889091382d76a43c146484fd1173881
    secrets: inherit
